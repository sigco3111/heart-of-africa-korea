# Lektion → Mechanismus: das Entscheidungsregister

Zu jeder Lektion der Retrospektive (`retrospektive-zusammenarbeit.md`, Abschnitt 3)
steht hier **eine erfasste Entscheidung** über ihren Mechanismus. Grund: Eine
Lehre, die nur aufgeschrieben ist, ist nicht befolgt — §1 des Nachbardokuments
sagt genau das, und eine Regel, die lediglich fordert „jede Regel braucht einen
Mechanismus", ist selbst wieder nur eine Regel. Was sie wirksam macht, ist eine
je Lektion **festgehaltene Entscheidung** plus etwas, das ihr Fehlen bemerkt.

Das Bemerken übernimmt `retro-currency-guard` (Kern: `scripts/retro-core.mjs`,
`evaluateLedger`). Eine Lektion ohne Zeile hier blockiert das Zug-Ende — genau in
dem Moment, in dem die Entscheidung noch billig ist.

## Die drei zulässigen Ergebnisse

| Ergebnis | Bedeutung |
|---|---|
| **1** | Ein **bestehender** Durchsetzer wurde erweitert/angepasst — das **bevorzugte** Ergebnis. |
| **2** | Ein **neuer** Durchsetzer, nur wo kein bestehender passt. |
| **3** | **Bewusst keiner**, mit geschriebener Begründung — die ehrliche Antwort auf eine Lehre, die keine Maschine prüfen kann („sieht das für einen Menschen richtig aus?"). |

Die Reihenfolge der Fragen bei jeder neuen Lektion, gegen den Wildwuchs
(§6 „Guard-Wildwuchs"): Deckt ein **bestehender** Durchsetzer das schon ab? Lässt
sich einer **verbreitern**, statt einen Geschwister-Guard danebenzustellen? Macht
diese Lektion eine **ältere Regel überflüssig**, sodass der Bestand *schrumpft*?
Ein neuer Guard ist das letzte Mittel. Eine Entscheidung, die zwei Regeln zu
einer zusammenführt, ist die bestmögliche Antwort.

## Was die Prüfung beweist — und was nicht

Sie beweist, dass **eine Entscheidung existiert** und dass ein benannter
Durchsetzer **auf eine reale Datei zeigt**. Das schließt genau den Defekt, den
`docs/rule-corpus-audit.md` in Zeile A29 festhält: eine Regel, die eine nie
gebaute Durchsetzung *behauptet*.

Sie beweist **nicht**, dass der genannte Durchsetzer die Lektion inhaltlich
*abdeckt*. Das kann keine billige Prüfung: Eine Namens-Heuristik würde
Fehlalarme produzieren (zwischen `render-verify-guard` und „Grüner Test, falsches
Bild" gibt es keine gemeinsame Zeichenkette), und Fehlalarme erziehen dazu, den
Durchsetzer zu umgehen (§3.32). Die Deckung bleibt daher **prüfungsgetragen** —
sie gehört in die periodische Regel-Durchsicht (`scripts/rule-review-guard.mjs`)
und in die Vier-Augen-Pflicht für Mechanismen, die seit dem 27.07.2026 selbst
erzwungen ist (`scripts/mechanism-review-guard.mjs`, 3.13/3.19). Dieser Vorbehalt steht hier, weil die falsche
Sicherheit nicht aus der Grenze entsteht, sondern aus dem Verschweigen der Grenze.

## Lücken werden gemeldet, nicht stillschweigend zu einer „3"

Eine Lektion **ohne jede Durchsetzung** ist keine bewusste Entscheidung gegen
einen Mechanismus. Solche Zeilen tragen die Marke `LÜCKE:` in der Begründung,
sind maschinell auszählbar (`ledgerGaps`) und stehen unten noch einmal als Liste.

---

## Register

| Lektion | Titel | Ergebnis | Durchsetzer / Begründung |
|---|---|---|---|
| 3.1 | Der Batch, der stehen blieb | 2 | `scripts/batch-progress-guard.mjs`, `scripts/batch-resume-hook.mjs` — sechs Lösungsgenerationen, die letzte war der blockierende Guard. |
| 3.2 | Parallele Sessions — Fix-of-Fix auf Prozessebene | 2 | `scripts/batch-singleton.mjs`, `scripts/lock-heartbeat-hook.mjs`, `scripts/batch-doctor.mjs` — harte Exklusivität am OS-Fakt plus Repo-Heilung. |
| 3.3 | Berechtigungs-Rückfragen | 3 | Bewusst keiner: Die Lösung war eine **Konfiguration** (breite Whole-Tool-Allows in `.claude/settings.json`), kein Verhalten. Eine ausgebliebene Rückfrage ist maschinell nicht beobachtbar, und Settings-Edits fragen immer nach — ein Guard hätte nichts zu prüfen. |
| 3.4 | Das Dashboard: Aktualität und Formtreue | 2 | `scripts/dashboard-guard.mjs`, `scripts/dashboard-integrity-guard.mjs`, `scripts/dashboard-conciseness-guard.mjs`, `scripts/dashboard-card-topic-guard.mjs` — ein Prüfer je Vertragsklausel, weil die Klauseln einzeln fielen. |
| 3.5 | „Grüner Test, falsches Bild" — die gefährlichste Falle | 1 | `scripts/render-verify-guard.mjs` deckt die **sichtbare** Klasse ab (Bild auf beiden Backends). Der allgemeine Satz „reales Signal, erreichbarer Zustand" bleibt urteilsgetragen — `docs/rule-corpus-audit.md` D-l hält fest, dass die nicht-visuellen Klassen offen sind. |
| 3.6 | Backend-Divergenz WebGPU/WebGL2 | 2 | `scripts/render-verify-guard.mjs`, `scripts/verify/tiers.mjs` — ein stiller Backend-Fallback schlägt seit `assertBackend` laut fehl. |
| 3.7 | Feature-Regressionen im Spielcode | 1 | `scripts/pre-push-gate.mjs` erzwingt das schnelle Gate vor jedem Push auf den ausgelieferten Zweig — die Regel „Fast-Gate nach **jedem** Merge" hing vorher am Gedächtnis. Die Exit-Pfad-Testdisziplin selbst bleibt Auftragsregel. |
| 3.8 | Flakes unter Last — „ruhige Maschine" | 3 | `LÜCKE:` Zwei Hälften, und nur eine ist wirklich entschieden. Die **Urteils**-Hälfte („gleiche Fehlschlagmenge zweimal = echtes Signal") ist bewusst ohne Mechanismus — ein Guard müsste die Systemlast der Nutzer-Maschine messen und ein Rot verwerfen, also genau Messung und Urteil vermischen (§3.22). Die **Zeitschranken**-Hälfte ist es nicht: `docs/rule-corpus-audit.md` D-h führt lastabhängige statt fest verdrahteter Zeitschranken als gebaut-werden-müssend und ungebaut. |
| 3.9 | „Wieso muss ich dich auf Bugs hinweisen?" | 3 | Bewusst keiner: Das QS-Framework (`docs/maximum-qa.md`) ist eine **Phasenreihenfolge**, kein prüfbarer Zustand; seine harte Kante ist der Closing-Vollständigkeits-Gate (§3.15). Die Pflichtfrage „sieht das für einen Menschen richtig aus?" ist per Definition menschlich. |
| 3.10 | Kleinere, aber lehrreiche Klassen | 3 | Sammelabschnitt aus sechs Kleinklassen. Die durchgesetzten Teile liegen bei ihren eigenen Lektionen (Deploy-Hygiene → `scripts/pre-push-gate.mjs`, Doku-Drift → 3.21, Token-Budget → 3.31). Eigenständig bleiben hier gemessene-statt-geschätzte Zahlen, Kommunikationsregeln und stille Verschlechterung — alle drei prüfen ein Urteil, keinen Zustand. |
| 3.11 | Nachweise sind zustandsgebunden | 1 | `scripts/render-verify-state.mjs`, `scripts/render-verify-guard.mjs` — der Nachweis war schon HEAD-gebunden; die Lektion schärfte den **Umgang** damit (gegen den Zielzustand laufen lassen), nicht den Mechanismus. |
| 3.12 | Ein Test kodiert eine veränderliche Vorgabe fest | 3 | Bewusst keiner: Ob eine Schwelle gegen den ausgelieferten Default oder gegen einen alten kalibriert wurde, steht in keiner Datei — es ist die Herkunft einer Zahl. Prüfbar ist nur das Vorgehen (Baseline auf dem Vor-Änderungs-Stand), und das ist eine Handlung, kein Zustand. |
| 3.13 | Modell-Diversität nach Kritikalität | 2 | `scripts/mechanism-review-core.mjs`, `scripts/mechanism-review-guard.mjs`, `scripts/mechanism-review.mjs` — der Bau, den `docs/rule-corpus-audit.md` D-b beschreibt: Ein neuer oder geänderter Durchsetzer (Guard, Gate, Git-Hook, Kern daneben) beendet den Zug erst, wenn eine Gegenprüfung erfasst ist — welches Modell, welches Ergebnis, welcher Stand. Das autorierende Modell wird abgelehnt statt gewarnt. **Ein** Durchsetzer für 3.13 und 3.19. |
| 3.14 | Fast-Gate ≠ Release-Gate | 1 | `scripts/closing-guard.mjs`, `scripts/pre-push-gate.mjs` — der bestehende Closing-Gate deckt die Auslieferungsseite ab; die Lektion begründet, warum er nicht durch das schnelle Gate ersetzbar ist. |
| 3.15 | Vollständigkeit eines Prozesses braucht ein Gate | 2 | `scripts/closing-guard-core.mjs`, `scripts/closing-guard.mjs` — kein Versions-Tag, solange ein Closing-Schritt unbelegt ist. |
| 3.16 | Mechanismus ZUERST — das übergeordnete Prinzip | 2 | `scripts/retro-core.mjs` (`evaluateLedger`) + `scripts/retro-currency-guard.mjs` + dieses Register. Die Meta-Regel war bis zum 27.07.2026 selbst nur eine Regel; sie ist jetzt je Lektion eine erfasste, geprüfte Entscheidung. |
| 3.17 | Stille Modell-Degradation — der Arbeiter selbst kann das Problem sein | 2 | `scripts/model-guard-core.mjs`, `scripts/model-guard.mjs` — die Identität des ausführenden Modells als überwachte Laufzeit-Invariante, gelesen aus den Commit-Trailern. |
| 3.18 | „Erfolgreich" ist nicht „angekommen" | 2 | `scripts/push-arrival-core.mjs`, `scripts/push-arrival-guard.mjs` — kein Zug-Ende, solange Commits in keiner Remote-Ref liegen. |
| 3.19 | Vier Augen finden, was ein Modell nicht sehen kann | 2 | Derselbe Durchsetzer wie 3.13 (`scripts/mechanism-review-guard.mjs`) — der Bestand wuchs um eins, nicht um zwei. Ein `do-not-merge`-Ergebnis blockiert so laut wie ein fehlender Eintrag; was vor der einmaligen Basislinie liegt, ist bestandsgeschützt, wie bei `scripts/model-guard.mjs`. Die Prüfung fand am eigenen Bau prompt fünf Befunde, darunter einen still umgehbaren Pfad. |
| 3.20 | Aufräumen ist eine Prüfaufgabe, keine Fleißaufgabe | 3 | `LÜCKE:` Kein Durchsetzer für den **Zwischenfall**-Fall. Die Form existiert und ist erzwungen — `scripts/closing-guard-core.mjs` führt eine belegpflichtige Schrittliste —, aber nur für eine Auslieferung; nach der Modell-Degradation und dem Doppel-Session-Vorfall lief das Aufräumen aus dem Gedächtnis (`docs/rule-corpus-audit.md` D-k). Die billige Fassung ist eine zweite Schrittliste im selben Guard. |
| 3.21 | Ein Fakt an fünf Stellen veraltet an vier davon | 1 | `src/config/qualityDoc.test.ts` ist das Muster (Prosa gegen den Code geprüft, der den Fakt besitzt); `scripts/retro-currency-guard.mjs` hält dieses Dokumentenpaar aktuell. Verallgemeinert ist es nicht: `docs/rule-corpus-audit.md` D-d hält vier lebende Drifts dieser Klasse fest, alle im Memory-Korpus, den kein Test erreicht. |
| 3.22 | Der rote Test, der den Unschuldigen anklagt | 1 | `scripts/batch-doctor.mjs` + `scripts/batch-doctor-core.mjs` (`judgeGateRun`, Punkt 431, 29.07.2026) — erweitert, nicht neu: Dort, wo eine **Maschine** die Anklage schreibt, ist sie jetzt geprüft. Das Doktor-Tor las dreimal an einem Nachmittag „die konkurrierenden Schreibzugriffe haben es zerbrochen", während es mit dem Build eines delegierten Agenten um die Maschine konkurrierte; es liest die Last nun je Befehl (Ruhe-Prüfung aus Punkt 296 plus lebende Agenten-Worktrees) und stuft ein Rot unter Last als UNENTSCHIEDEN ein — benennt, was lief, und bittet um Wiederholung, statt den Batch anzuhalten. Nur ein Rot auf gemessen ruhiger Maschine klagt an, und es steht im Protokoll zuerst. **Grenze:** dieselbe Frage vor einem Code-Edit eines Menschen bleibt urteilsgetragen (`docs/rule-corpus-audit.md` D-e) — durchgesetzt ist der automatisierte Ankläger, nicht die Haltung. |
| 3.23 | Eine Regel zurückzunehmen ist teurer als sie aufzustellen | 1 | `scripts/rule-review-guard.mjs`, `scripts/rule-review-core.mjs` — die periodische Durchsicht ist der Ort, an dem Dopplungen und halb zurückgenommene Regeln auffallen. Ein eigener Guard neben ihr wäre genau der Anbau, den die Lektion beklagt. |
| 3.24 | Zweige verfallen — in Stunden, nicht Tagen | 3 | Bewusst keiner: Ein Zweigalter-Gate würde legitime lange Arbeit blockieren. Die teure Folge ist ohnehin gedeckt — `scripts/render-verify-guard.mjs` bindet den Nachweis an den Zustand, gegen den er lief, sodass ein veralteter Zweig eine erneute Prüfung erzwingt statt still durchzurutschen. |
| 3.25 | Der Regelbestand verrottet wie Code — nur unbemerkt | 2 | `scripts/rule-review-guard.mjs` (periodischer Zwang) und `scripts/guard-health-guard.mjs` (kein Durchsetzer, den nichts aufruft). Zusätzlich bricht `scripts/retro-sources.mjs` bei leerem Quellverzeichnis laut ab — der „still leer geschriebene Anhang" aus Punkt 4 der Lektion. |
| 3.26 | Ein Dokument driftet in die Rolle des Nachbardokuments | 2 | `scripts/guide-brevity-core.mjs`, `scripts/guide-brevity-guard.mjs` — Budgets je Fallstrick plus ein Detektor für Projekterfahrungs-Marker; die Fehlermeldung fordert **hinüberzukürzen**, statt das Budget zu heben. |
| 3.27 | Verbrauch pro Zeit ist nicht Verbrauch pro Arbeit | 3 | Bewusst keiner: Der Fehler war ein **Denkfehler** in einer Kostenrechnung (falscher Nenner). Prüfbar wäre höchstens, ob eine Zahl gemessen ist — und auch das nicht, siehe `docs/rule-corpus-audit.md` D-g. |
| 3.28 | Die teuerste Prüfung war die unschärfste | 1 | `scripts/render-verify-core.mjs` (`isBackendSensitivePath`), `scripts/render-verify-guard.mjs` — der bestehende Guard wurde **verengt**, nicht ersetzt: zwei Backends nur noch dort, wo sie verschieden zeichnen können. |
| 3.29 | Der Arbeitsauftrag wuchs, bis er sich selbst im Weg stand | 2 | `scripts/tasks-archive-guard-core.mjs`, `scripts/tasks-archive-guard.mjs`, `scripts/tasks-source.mjs` — offen in `TASKS.md`, erledigt im Archiv, und beide Hälften über einen Leser, der weiß, wer welche braucht. |
| 3.30 | Dieselbe Kurve beim Regeldokument — und was das Aufräumen selbst kostete | 2 | `scripts/doc-budget-core.mjs`, `scripts/doc-budget-guard.mjs` — gemessene Obergrenzen mit genau zwei zulässigen Auswegen: auslagern oder die Grenze mit schriftlicher Begründung anheben. |
| 3.31 | Die Rechnung stimmte, ihre Voraussetzung nicht — gemessene Verbrauchstreiber | 2 | `scripts/point-brief.mjs` (Zustellung statt Suche) und `scripts/guard-preflight.mjs` (die Bedingung eines Wächters vorher prüfen, statt einen vollen Zug hineinzulaufen). Die dritte Lehre — die Sitzungsgrenze — ist ein **Nutzerbefehl** und deshalb hier nicht durchsetzbar. |
| 3.32 | Ein Durchsetzer, der zu spät greift — und einer, der zu früh anschlägt | 2 | `scripts/board-first-core.mjs`, `scripts/board-first-guard.mjs` — das Versprechen über den *laufenden* Zustand wird jetzt vor der Arbeit durchgesetzt (PreToolUse), nicht am Zug-Ende. Die Fehlalarm-Hälfte sitzt als Wortgrenze in `scripts/tasks-spec-guard-core.mjs`. |
| 3.33 | Eine Ersparnis, die Nacharbeit auslöst, ist keine Ersparnis | 3 | Bewusst keiner: eine **Kostenregel** für Entscheidungen über Maßnahmen, kein prüfbarer Repo-Zustand. Ihr konkretester Fall — der zu schwache Arbeiter — ist bei 3.17 durchgesetzt. |
| 3.34 | Die Attrappe, die den Fehler verdeckt | 2 | `scripts/guard-hooks.test.mjs` — die Wächter werden gestartet, wie die Umgebung sie startet (echter Prozess, echtes stdin, echter Verdikt), statt hinter Attrappen geprüft. Die allgemeine Fassung („jedes real ausgeführte Kommando braucht einen Test, der es ausführt") ist damit für die Guard-Kette erfüllt, für beliebigen Code nicht. |
| 3.35 | Der beabsichtigte Zustand, im Präsens geschrieben | 3 | `LÜCKE:` Kein Durchsetzer. Die Lektion verlangt, dass eine im **Präsens** geschriebene Zusicherung über den Code nachgesehen ist — `docs/rule-corpus-audit.md` D-i führt genau das als ungebaut. Das laute Scheitern des Kurzbriefs an einem toten Verweis deckt die **Referenz**-Klasse ab, und die gehört zu 3.37; für die Behauptungs-Klasse dieser Lektion ist die Deckung null. (Erste Fassung dieser Zeile buchte sie als „1" — die Gegenlesung hat es gefunden, und es ist genau der Fehler, den dieses Register verhindern soll.) |
| 3.36 | Isolierung ist eine Eigenschaft der Umgebung, keine Anweisung | 1 | Der eigentliche Durchsetzer ist die **Worktree-Isolierung der Delegation selbst** — eine Eigenschaft der Umgebung, die einen Schreibzugriff außerhalb des eigenen Baums verweigert (beim Bau dieses Registers zweimal live erlebt). Sie liegt außerhalb des Repos und ist deshalb hier nicht maschinell prüfbar. `scripts/worktree-reminder-core.mjs` ist die Repo-Hälfte und **blockiert nichts** (Kopfkommentar Zeile 12), sie spielt die Regel nur ein — als alleiniger Beleg wäre sie genau die zahnlose Behauptung, gegen die dieses Register gebaut ist. |
| 3.37 | Ein Werkzeug, das rät, ersetzt still | 1 | `scripts/point-brief-core.mjs` — jeder mitgelieferte Abschnitt trägt sein Herkunftsdokument, eine Referenzkarte nennt jede Auflösung, und was nirgends aufgeht, scheitert unter Nennung aller durchsuchten Dokumente. |
| 3.38 | Fail-open EINMAL ist nicht fail-open FÜR IMMER | 1 | `scripts/render-verify-core.mjs`, `scripts/render-verify-guard.mjs` — derselbe Guard, repariert an der entscheidenden Stelle: Er lässt im Fehlerfall durch, schreibt aber keinen Zustand fort; eine unbeantwortbare Frage zählt als „vorhanden". |
| 3.39 | Neun Ausfälle an einem Tag — und was den Schaden bestimmt hat | 1 | `scripts/push-arrival-guard.mjs` sichert die Hälfte „sofort gesichert": Kein Zug endet, solange Commits in keiner Remote-Ref liegen. Die andere Hälfte — nach **jedem** zusammenhängenden Schritt committen — hat **keinen** Mechanismus: Sie steht in handgeschriebenen Delegations-Prompts, nicht im generierten Kurzbrief (`scripts/point-brief-core.mjs` enthält keine solche Zeile). Die erste Fassung dieser Zeile behauptete das Gegenteil; die Gegenlesung hat es nachgeschlagen und widerlegt. Siehe Teil-Deckungen unten. |
| 3.40 | Eine Prüfung, die zu spät kommt, ist eine Benachrichtigung | 2 | `scripts/pre-push-gate-core.mjs`, `scripts/pre-push-gate.mjs`, `scripts/git-hooks/pre-push`, verdrahtet über `scripts/enable-hooks.mjs` — die Prüfung sitzt jetzt **vor** der Handlung, und ein Test prüft die Verdrahtung selbst. |
| 3.41 | Ein Ergebnis, das kein Beweis ist | 2 | `scripts/verify/machine-load-core.mjs` + `scripts/verify/machine-load.mjs` urteilen **vor** dem Lauf über die Maschine und kennzeichnen ein zeitkritisches Rot als nicht beweiskräftig; `scripts/verify/baseline-classify-core.mjs` + `scripts/verify/baseline-classify.mjs` ordnen **nach** dem Lauf ein (dieselbe Prüfung zweimal rot = Verdacht, verschiedene = Last) und vergleichen auf Wunsch gegen den Stand vor der Änderung. Beide beschriften statt zu blockieren, mit der Asymmetrie: ein Grün unter Last zählt, ein zeitkritisches Rot nicht. |
| 3.42 | Erlaubt ist nicht genommen | 1 | `scripts/batch-progress-guard.mjs` — der bestehende Guard wurde um den Vollzug erweitert: Eine erlaubte, aber nicht genommene Grenze BLOCKIERT jetzt und nennt den einen Befehl, und der freigegebene Halt trägt die Übergabe in die Sperre ein (`markHandover`), so dass der Starter den Nachfolger startet. Am 28.07.2026 einmal vollständig beobachtet — die fünf Glieder stehen in `docs/batch-autonomy.md`. |
| 3.43 | Der Fehler, den die Fail-open-Hülle verschluckt | 1 | `scripts/batch-progress-guard.mjs` + `scripts/atomic-write.mjs` — die Reihenfolge ist umgedreht (der Marker überlebt seinen eigenen Gebrauch), das Scheitern des Schreibens wird IN der Freigabe gemeldet statt verschluckt, der Schreibvorgang wiederholt einen Windows-Rechte-Fehler, und `statePathsFor` leitet jeden Zustandspfad aus der übergebenen Sperre ab, so dass kein Testlauf mehr in den Live-Zustand schreibt. |
| 3.44 | Das Messfenster war schmaler als das Gemessene — dreimal an einem Tag | 3 | TEILWEISE: Für das Prüfwerkzeug ist es geschlossen — `scripts/batch-handover-observe-core.mjs` ankert an der Übergabe und fragt `closureOf`, einen Zustand statt eines Ereignisses im Fenster. LÜCKE bleiben die beiden anderen Instrumente derselben Klasse: die Fälligkeitsprüfung der Sitzungsgrenze (`boundaryDueFrom` über `lastWorkOrderTick`, Arbeitsauftrag 399) und die Lastmessung des Push-Tors, die erst NACH dem Lauf fragt (Arbeitsauftrag 398). Bis dahin erfasst und ausdrücklich NICHT durchgesetzt. |
| 3.45 | Die Prüfung stand hinter der Auslieferung — und derselbe Griff brach dreimal | 2 | `scripts/board-structure-core.mjs`, aufgerufen in `scripts/dashboard-publish.mjs` VOR der Kopie, die der Artifact-Aufruf veröffentlicht: Tag-Bilanz, die vier Abschnitte in ihrer Reihenfolge, keine verwaiste Abschnittshülle, jede Karte der laufenden Arbeit im richtigen Abschnitt. Ein zerbrochenes Board ist damit nicht mehr veröffentlichbar; die drei realen Bruchformen des 28.07.2026 sind Testfälle. Der bestehende Konsistenz-Audit bleibt zuständig für Inhalt und Aktualität — er läuft weiterhin bei `--synced`, also danach. |
| 3.46 | Dreitausend bestandene Tests, und die Hälfte lief nie | 3 | LÜCKE: Nichts vergleicht die Zahl der AUSGEFÜHRTEN Testdateien mit dem letzten bekannten Stand — ein grünes Ergebnis über einer stillschweigend geschrumpften Menge passiert jedes Tor. Der billigste Ort wäre `scripts/pre-push-gate.mjs`, das den Lauf ohnehin auswertet: die Dateizahl mitschreiben und einen Rückgang wie ein Rot behandeln. Erfasst als Arbeitsauftrag 404, bis dahin ausdrücklich NICHT durchgesetzt. |
| 3.47 | Die Prüfung, die auf der schnellen Maschine scheitert | 2 | `scripts/verify/polish.mjs` — die Messfenster werden in SCHRITTLÄNGEN des gemessenen Tieres gebildet statt in absoluten Welteinheiten, die Akzeptanzschwelle ist ein Anteil der Schrittlänge, und eine leere Messung meldet „MEASURED NOTHING" statt „Unendlich". Dieselbe Umstellung von Wanduhrzeit auf gerenderte Bilder in drei Nachbarprüfungen; `fixed-wait-baseline.json` von 34 auf 32 gesenkt. |
| 3.48 | Zweimal rot heißt nur dann „nicht die Last", wenn die Last dazwischen weg war | 2 | LÜCKE: `scripts/pre-push-gate.mjs` behauptet nach dem zweiten Rot, die Last sei ausgeschlossen, obwohl sie zwischen beiden Läufen unverändert bestand — dreimal in einer Nacht bei vollständig grünen Tests. Der Umbau ist Teil des Arbeitsauftrags 404 (das Tor wertet den Lauf dann ohnehin aus und kann die Signatur „alles bestanden, Prozess trotzdem rot" benennen); bis dahin ausdrücklich NICHT durchgesetzt. |
| 3.49 | Aufräumen, das durch eine Verknüpfung hindurchlöscht | 1 | LÜCKE: Kein Durchsetzer prüft heute, ob eine Löschoperation einer Verknüpfung folgt. Gefangen hat den Schaden allein `scripts/pre-push-gate.mjs`, unmittelbar danach und zufällig — das ist Schadensbegrenzung, keine Verhinderung. Erfasst als Arbeitsauftrag 409, der das Löschen des Arbeitsbaums in den Merge zieht und ihm verbietet, einer Verknüpfung zu folgen; bis dahin ausdrücklich NICHT durchgesetzt. **Am 29.07.2026 ist genau dieser Schaden zweimal an einem Nachmittag wiedergekehrt** (zwei Agenten, zwei verschiedene Löschkommandos, beide Male das `node_modules` des Hauptbaums) — die Lücke ist damit nicht mehr theoretisch, und Arbeitsauftrag 429 hat sie GESCHLOSSEN (29.07.2026): Ursache (i) ist belegt — das `node_modules` des Arbeitsbaums ist eine Junction in den Hauptbaum, und `git worktree remove --force` löscht hindurch (auf einem Wegwerf-Repository reproduziert). MECHANISMUS: `scripts/worktree-cleanup.mjs` — EIN Aufräumbefehl, der zuerst jede Verknüpfung im Baum ablöst und erst dann entfernt, der den Hauptbaum und jeden Pfad außerhalb des Arbeitsbaums verweigert; `scripts/worktree-cleanup-core.test.mjs` prüft, dass das Verknüpfungsziel überlebt, samt NEGATIVKONTROLLE, die den Schaden mit dem nackten git-Befehl reproduziert. CLAUDE.md §6 und docs/batch-autonomy.md nennen den Befehl als den einzigen; `scripts/deps-preflight.mjs` erklärt den Folgeschaden am Build. |
| 3.50 | Der Zustand, den nur die Lücke zwischen zwei Schritten erzeugt | 3 | LÜCKE: Kein Wächter kann einen Zustand sehen, der nur ZWISCHEN zwei Schreibvorgängen existiert — die Stop-Kette prüft am Zugende, da ist die Lücke längst geschlossen. Gefangen hat ihn beide Male der Nutzer, und danach `scripts/board-core.test.mjs` als Rot-Färbung des ganzen Laufs. Erfasst als Arbeitsauftrag 416, der Archivieren und Hochziehen zu EINEM Schreibvorgang macht; bis dahin ausdrücklich NICHT durchgesetzt. |
| 3.51 | Die Ebene darunter wird erst sichtbar, wenn die darüber gut wird | 3 | KEIN MECHANISMUS MÖGLICH und keiner nötig: Dies ist eine Deutungsregel für den Menschen, kein durchsetzbarer Zustand. Was durchsetzbar ist, existiert bereits — die Zuordnung „neu gebaut, also neu kaputt" wird am Verlauf geprüft (`git log`/`git show` auf die fragliche Datei), und genau das hat die Vermutung hier widerlegt. |
| 3.52 | Der Ausweg, den die Verweigerung nennt, gibt es nicht | 3 | LÜCKE: Nichts prüft, ob der von einer Verweigerung genannte Ausweg im auslösenden Zustand wirklich begehbar ist — gefunden hat es allein die Vier-Augen-Prüfung (`scripts/mechanism-review.mjs`), die dieses Projekt fuer jeden Mechanismus ohnehin verlangt. Erfasst als Teil des Arbeitsauftrags 400, der das fehlende Werkzeug baut und in der Meldung nennt; bis dahin ausdrücklich NICHT durchgesetzt. |
| 3.53 | Der Schreiber und der Prüfer kannten dieselbe Regel verschieden | 3 | DURCHGESETZT, im Code statt in einem Wächter: `scripts/board-core.mjs` importiert `QUEUE_STUB_META` aus `dashboard-guard-core.mjs`, statt den Wert ein zweites Mal zu formulieren — die Divergenz ist damit nicht mehr formulierbar, nicht nur nicht mehr erlaubt. Festgenagelt in `scripts/board-core.test.mjs` (der Zug ohne Zeitspanne erzeugt die benannte Attrappe und keinen `queue-meta`-Verstoß), und der bestehende Live-Board-Durchlauf desselben Tests hält die Klasse offen: Er fährt jeden Board-Zug gegen das ECHTE Board und färbt den Lauf rot, sobald ein Zug ein prüfungswidriges Artefakt erzeugt — genau so ist dieser Fehler aufgefallen. |
| 3.54 | Der Beweis, den eine ungeprüfte Anweisung schreibt | 2 | TEILWEISE DURCHGESETZT: Der Wächter selbst existiert und greift (`scripts/model-guard.mjs` mit der Erlaubnisliste in `scripts/model-guard-core.mjs`) — was fehlt, ist die Unterscheidung zwischen einem UNGENANNTEN und einem VERBOTENEN Autor, die den Fehlalarm überhaupt erst erzeugt hat. Erfasst als Arbeitsauftrag 397, der genau diese Trennung baut. Die zweite Hälfte — dass ein Delegations-Prompt die geprüfte Angabe nicht neu formuliert — liegt heute in der Delegationsregel und ist NICHT durchgesetzt: Kein Werkzeug liest den Prompt, mit dem ein Agent beauftragt wird. |
| 3.55 | Was beim Umzug still zurückbleibt | 2 | DURCHGESETZT für drei der vier Verluste: `scripts/board-structure-core.mjs` verweigert die Veröffentlichung einer Tafel ohne eigenes Viewport-Meta (Regel `viewport-missing`, festgenagelt in `scripts/board-structure-core.test.mjs`); der Selbst-Auffrischer liegt als versionierte Quelle in `scripts/board-refresher-core.mjs` und wird in `scripts/board-refresher-core.test.mjs` in jsdom wirklich ausgeführt — der Fall „Antwort ohne `<main>`", der den Ausfall verbarg, ist dort ein eigener Test; und `scripts/dashboard-guard-core.mjs` meldet über `queue-stubbed`, wenn die Warteschlange zu Platzhaltern verkommt. NICHT durchgesetzt ist die vierte, die REIHENFOLGE: Sie steht in der Warteschlangen-Datendatei unter .claude/, die keiner Versionsverwaltung untersteht, und kein Wächter merkt, wenn sie durch die Nummernfolge ersetzt wird. |
| 3.56 | Die Marke, die ein Seitenstrich zurücknahm | 1 | GEBAUT (Arbeitsauftrag 426, 30.07.2026): der BESTEHENDE Durchsetzer wurde erweitert, kein neuer danebengestellt. `scripts/batch-boundary-core.mjs` (`isClosingSetCommand` + `isOutputPagerSegment`) lässt einen reinen Pager (`head`/`tail`/`more`/`cat`) am ENDE einer Abschluss-Zeile stehen — die Ausgabe kürzen ist kein Weiterarbeiten —, und zwar in der engsten möglichen Fassung: in der MITTE zählt ein Pager weiter als Arbeit, allein ist er keine Abschluss-Zeile, und das Verbot undurchsichtiger Segmente (`$(…)`, Backticks, `>`, `<`) blieb unangetastet, denn die gefährliche Richtung ist eine BEHALTENE Grenze neben echter Arbeit. Zweite Hälfte: jede Rücknahme einer genommenen Marke schreibt „MARKER WITHDRAWN for point N … triggered by <Aufruf>" nach `.claude/boundary.log` (`describeWithdrawalTrigger`, best effort, bricht nie einen Werkzeugaufruf) — das stille Löschen war der eigentliche Schaden. Vitest: `scripts/batch-boundary-core.test.mjs`, `scripts/batch-singleton-core.test.mjs`. |
| 3.57 | Die Anleitung an den Nutzer ist die schlechteste aller Antworten | 3 | BEWUSST KEIN WÄCHTER, und das ist hier die ehrliche Antwort: Eine Maschine kann nicht sehen, ob eine Antwort den Nutzer zu Handgriffen lenkt, die ein Repository-Kommando erledigt hätte — sie kennt weder die Absicht der Antwort noch die Menge der möglichen Wege. Verankert wird die Lehre dort, wo sie im richtigen Moment gelesen wird: als Regel in CLAUDE.md §6 („Let a subprocess answer" wird um die blockierte Aktion erweitert) und als Memory-Eintrag `blocked-action-find-the-project-command`. Die eine maschinell prüfbare Hälfte — dass eine ANFRAGE an den Nutzer als Karte sichtbar wird statt nur im Chat zu stehen — ist bereits als Arbeitsauftrag 421 erfasst (`decision-card-guard`). |
| 3.59 | Der Nachweis reichte nur so weit wie die damalige Freiheit | 1 | BEWUSST KEIN WÄCHTER, mit einer benannten prüfbaren Hälfte: Keine Maschine kann entscheiden, welche bestehenden Zusagen eine neue Blick-, Bewegungs- oder Zoomachse rückwirkend vergrößert — dazu müsste sie das Kriterium verstehen, nicht nur seinen Text lesen. Verankert wird die Lehre in CLAUDE.md §7.2, wo die Regel „an in-game erreichbaren Zuständen prüfen" bereits steht und um ihre Kehrseite gehört: Wächst die erreichbare Menge, altern die Nachweise, die INNERHALB der alten Menge geführt wurden. Prüfbar ist der konkrete Fund — Arbeitsauftrag 428 misst die Helligkeitsstufe an der Naht innerhalb EINES Bildes (kein Bildvergleich zwischen Läufen, den die Hausregel verbietet) und nagelt in `src/scenes/place/backdrop.test.ts` fest, dass begehbarer Boden und Hintergrund dieselben Beleuchtungsgrößen auflösen. |
| 3.63 | Drei Wochen rote Pipeline, und gemerkt hat es das Postfach | 1 | Ein BESTEHENDER Durchsetzer wird erweitert: `scripts/ci-status-guard.mjs` prüft heute den HEAD der eigenen Sitzung und war deshalb blind für die Zweige, die delegierte Agenten pushen. Erfasst als Arbeitsauftrag 387 (zweite Hälfte): der Wächter beurteilt JEDEN von dieser Sitzung gepushten Ref, den er noch nicht grün gesehen hat, benennt ihn, und meldet einmal je (Ref, Stand) statt je Zug — und die Forderung wird von „rot bemerken" auf „grün bestätigen" gehoben, weil nur das die Klasse schließt, die das lokale Tor nie sehen kann: Plattform-Unterschiede zwischen der schreibenden Maschine und dem Runner. Dazu die Testregel, die den Auslöser verursachte: ein Test über Betriebssystemverhalten behauptet PRO Plattform (`scripts/worktree-cleanup-core.test.mjs`), nie per Überspringen. |
| 3.64 | Die Erinnerung, die neben ihrem eigenen Tor stehen blieb | 1 | Ein BESTEHENDER Durchsetzer wurde erweitert und der Bestand SCHRUMPFT dabei — das bevorzugte Ergebnis in Reinform. `scripts/dashboard-reminder-core.mjs` hält den eingespritzten Text seit 30.07.2026 an zwei Bedingungen fest, die `scripts/dashboard-reminder-core.test.mjs` prüft: keine der in `ENFORCED_CLAIMS` gelisteten Behauptungen darf zurückkehren (jede nennt das Tor, das sie ersetzt), und keine der `UNENFORCEABLE_DUTIES` darf verschwinden — eine Kürzung, die eine echte Pflicht mitnimmt, wäre teurer als die Wiederholung. Dazu `REMINDER_CHAR_BUDGET` als das eigentliche Bollwerk gegen das Nachwachsen: Der Vier-Augen-Review zeigte, dass eine UMFORMULIERTE Wiederkehr an der Musterliste vorbeikommt, an der gemessenen Größe aber nicht. Die Klasse selbst — jeder Text, der eine maschinell erzwungene Regel wiederholt — ist als Arbeitsauftrag 440 erfasst: eine gemessene Inventur aller pro Eingabe und pro Sitzung eingespritzten Texte, mit der Regel, dass der Commit, der ein Tor einführt, den Text streicht, den dieses Tor ersetzt. |
| 3.65 | Der Rückfall, der nichts sagt | 1 | Ein BESTEHENDER Durchsetzer wird erweitert: Der Generator der Warteschlange (`scripts/board-queue-core.mjs`, `queueEntries`) ersetzt einen fehlenden Kartentitel still durch die Überschrift aus der Arbeitsliste, die per Projektregel englisch und in Versalien ist. Der Rückfall bleibt — eine namenlose Karte wäre schlimmer —, aber er wird gemeldet: als Arbeitsauftrag 439 beim Neubau der Warteschlange UND in der Prüfung vor dem Veröffentlichen, jeweils mit den betroffenen Punktnummern, und die Meldung nennt den Befehl, der sie behebt (`board.mjs title`), den es bis dahin gar nicht gab. Genau dieses Fehlen erzwang die Reparatur von Hand, die prompt die Tafel mit falschen Zeilenenden zurückschrieb — deshalb gehört der Befehl zum Mechanismus, nicht nur die Meldung. |
| 3.66 | Die Kennung, die nur innen etwas bedeutet | 3 | BEWUSST KEIN WÄCHTER für die Lehre selbst: Ob ein Name für den Leser etwas bedeutet, kann keine Maschine beurteilen — sie kann Zeichen zählen, nicht Verständlichkeit. Verankert ist die Regel dort, wo sie im Moment des Schreibens gelesen wird: Memory `bundle-names` hält die Zuordnung Buchstabe → gesprochener Name für alle dreizehn Pakete und die Pflicht, einem NEUEN Paket den Namen beim Schnitt zu geben. Die eine maschinell prüfbare Hälfte ist benannt und bewusst NICHT gebaut, weil sie den Guard-Bestand für einen Textfehler vergrößern würde: Ein „Bündel <Buchstabe>" in einer Tafelkarte oder einer Chat-Antwort ist per Muster auffindbar und gehört, falls die Klasse wiederkehrt, in die BESTEHENDE Kartenprüfung (`dashboard-conciseness-guard-core`) statt in einen eigenen Wächter. |
| 3.70 | Die Übergabe, die ein totes Fenster nicht einlösen konnte | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, kein neuer daneben: Die Übernahme-Entscheidung des Launchers (`scripts/batch-autostart-core.mjs`) hält den Anspruch schon heute in Ehren, solange er unverbraucht ist — die Log-Zeilen „skip: session … has CLAIMED the batch" belegen es. Was fehlt, ist die Schonfrist NACH der Freigabe: Sobald der Eigentümer für X freigegeben hat, ist der Anspruch verbraucht, und der nächste Tick nimmt den freien Lock für sich. Die Entscheidung: eine Abhol-Frist von zwei Ticks, in der der Launcher den freien Lock NICHT selbst nimmt, solange das Fenster von X lebt — dieselbe Lebendigkeitsprüfung, die Punkt 434 für den unverfallbaren Anspruch schon baut, nur an der anderen Seite der Naht. Erfasst im Bündel **Urlaubsfestigkeit**; die zweite, größere Hälfte derselben Lektion — jede Aktion mit zwei Hälften wird an ihrer Naht geprüft — wird dort zum Chaos-Drill mit Kills zu zufälligen Zeitpunkten, weil kein Wächter beweisen kann, was nur ein Ausfall zeigt. |
| 3.69 | Die Sonde, die ihren eigenen Blick misst | 3 | BEWUSST KEIN WÄCHTER für die Lehre selbst: „Ist das Gemessene wirklich das Ergebnis?" ist die Frage, die eine Maschine gerade nicht stellen kann — sie prüft den Stellvertreter, dessen Tauglichkeit der Zweifel gilt. Der Bestand deckt die eine Klasse, in der sich die Lehre billig prüfen ließ, bereits ab, und sie ist der Präzedenzfall: Der Bild-Verschluss (`scripts/verify/frameSubject.mjs`) verweigert einen Screenshot, dessen benanntes Motiv nicht im Bild ist, statt einem gerechneten Radius zu glauben. Für die konkrete Sonde bleibt der Nachweis ein Regressionstest — sie datiert einen Agenten seit 30.07.2026 an seinen BEARBEITUNGEN statt an Git-Metadateien, samt des neu angelegten Verzeichnisses, das der erste Anlauf noch durchließ. Ein eigener Wächter würde den Bestand um eine Regel vergrößern, die nur eine Formulierung prüfen könnte, nicht ihre Wahrheit (§6 „Guard-Wildwuchs"). |
| 3.71 | Das Verschlucken, gegen das kein Fail-Open hilft | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, kein neuer daneben: Die Fail-Open-Hülle der Doktor-Erhebungen (`scripts/batch-doctor.mjs`) ist bereits der richtige Mechanismus — sie war nur wirkungslos, weil ein inneres `catch` den Fehlschlag vorher in einen plausiblen Wert übersetzt hatte. Die Entscheidung: die Erhebungen reichen ihr Scheitern durch, und die zerstoerenden Reparaturen urteilen am AUSFÜHRUNGSZEITPUNKT ein zweites Mal über ihr Ziel (`scripts/batch-doctor-states.mjs`, Punkt 443) — beides seit 30.07.2026 gebaut und mit Fällen belegt, die ohne die Änderung rot sind. Für die allgemeine Form — welches `catch` in die sichere Richtung zeigt — bleibt bewusst kein Wächter: Ob ein Ersatzwert zu WENIGER oder zu MEHR Befunden führt, ist eine Bedeutungsfrage, die nur ein Review beantwortet, und genau dieses Review hat sie hier beantwortet (§6 „Guard-Wildwuchs"). Die zweite Hälfte der Lektion — ein als schlafend verbuchter Wächter ist gar keiner — ist als Teil (D) von Punkt 437 erfasst: Die Gesundheitsprüfung liest den Schlafend-Eintrag künftig auch für einen VERDRAHTETEN Wächter und meldet ihn als Verletzung. |
| 3.72 | Null Treffer sahen aus wie null Titel | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, kein neuer daneben: Der Rückfall-Melder aus 3.65 (Punkt 439, `scripts/board-queue-core.mjs`) ist bereits die richtige Stelle — er war nur wirkungslos, weil die Erhebung darüber schon leer ankam. Die Entscheidung: der Parser verträgt beide Zeilenendformen UND meldet, wenn er über einer nicht-leeren Quelle null Treffer hat; die Testvorlage führt bewusst Wagenrücklauf-Zeichen mit, weil eine Vorlage ohne sie heute besteht und nichts beweist. Für die allgemeine Form — welche leere Menge ein Befund und welche eine Antwort ist — bleibt bewusst kein Wächter: das ist eine Bedeutungsfrage je Erhebung, die nur ein Review beantwortet (§6 „Guard-Wildwuchs"). Die Ursache selbst ist mit der Angleichung der Arbeitsliste an die eigene Repository-Regel (`* text=auto eol=lf`) am 30.07.2026 beseitigt. |
| 3.73 | Der Schreiber war repariert, der Schaden nicht | 3 | BEWUSST KEIN WÄCHTER für die Lehre selbst: Ob ein Defekt Daten hinterlassen hat, hängt daran, WAS er geschrieben hat — das kann keine allgemeine Prüfung wissen. Was gebaut wurde, ist die konkrete Absicherung an der Fundstelle: `setQueueEntry` weist einen Kartenwert zurück, der ein blosses Befehlsflag ist (Punkt 439, mit Test), und die 31 beschädigten Texte wurden im selben Vorgang neu geschrieben statt einer späteren Beschwerde überlassen (Punkt 469). Die allgemeine Form gehört in die Durchsicht: Wer einen Schreiber repariert, zählt im selben Vorgang, wie viele Datensätze er in der Zwischenzeit erzeugt hat. |
| 3.74 | Eine Behauptung über die Zukunft ist prüfbar | 2 | Ein NEUER Durchsetzer, weil kein bestehender die Aussage kennt: Punkt 470 macht aus „gerade läuft nichts" eine ANKÜNDIGUNG ZU HALTEN — die PreToolUse-Tafelsperre `scripts/board-first-guard.mjs` (Kern `scripts/board-first-core.mjs`) verweigert die nächste zustandsändernde Handlung, solange die Tafel diese Behauptung trägt, und nennt als Abhilfe entweder eine Karte für die laufende Arbeit oder das Aufhören. Lesevorgänge, die Tafelbefehle selbst und der Sitzungsabschluss bleiben frei, die Sperre bleibt fail-open. Der Nutzer hat genau diese Form verlangt („nicht auf Zuruf korrigieren, sondern per Mechanismus"). |
| 3.75 | Der Umzug trennte das Projekt von seiner Mechanik | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, kein neuer daneben: `scripts/batch-doctor.mjs` beurteilt beim Sitzungsstart ohnehin schon, ob der Arbeitsbaum tragfähig ist, meldet einen Befund und repariert ihn — ihm fehlt nur eine zweite Frage, nämlich ob die UMGEBUNG vollständig ist. Die Entscheidung: derselbe Lauf prüft künftig die nicht-versionierten Voraussetzungen, jede gegen eine Beobachtung statt gegen eine Vermutung — ein nicht leeres Erinnerungsverzeichnis, ein lesbares Token an der Stelle, an der `ci-status-guard` zuerst nachsieht, ein scharfer Starter und ein startfähiger Browser für die Bildprüfung. Fehlt eines, ist das ein BEFUND wie ein zerrissener Baum, keine Stille. Die konkreten Reparaturen für diesen Umzug sind die Punkte 474 (Starter) und 475 (Browser); die Verbreiterung des Doktors ist die allgemeine Hälfte, damit der nächste Wechsel des Wirts sich meldet, statt sich einen halben Tag lang stückweise zu zeigen. Kein neuer Wächter — der Ort, der „ist hier alles in Ordnung?" beantwortet, existiert bereits und beantwortet die Frage bisher nur zur Hälfte. |
| 3.76 | Ein Urteil gehört dem Stand, den es gemessen hat | 1 | Ein BESTEHENDER Durchsetzer trägt es bereits, und er hat den Fall am 03.08.2026 selbst gefangen: Die Dateizählung des Push-Tors (`scripts/pre-push-gate.mjs`, Punkt 404) vergleicht die Zahl der AUSGEFÜHRTEN Testdateien mit der Zahl, die der Baum enthält, und verweigert den Push bei einer Differenz — hier 194 gegen 197, nachdem mitten im Lauf ein Zweig in denselben Baum gemergt worden war. Die Zählung war gegen eine beschädigte Abhängigkeitskette gebaut; sie deckt dieselbe Klasse ab, weil beide Fälle dasselbe Symptom erzeugen: ein bestandener Lauf über einer kleineren Menge als der, für die das Urteil gelten soll. Kein neuer Wächter, keine Verbreiterung nötig — die Lehre ist die Verallgemeinerung dessen, was der bestehende Durchsetzer misst. |
| 3.77 | Eine Priorität, die nur der Leser sieht, ist keine | 1 | Ein BESTEHENDER Durchsetzer wird erweitert: `queue-order-guard` beurteilt die Reihenfolge der Warteschlange ohnehin (Fixes vor Findern) — ihm fehlt die Frage, ob die Reihenfolge die DEKLARIERTE Priorität überhaupt abbildet. Die Entscheidung: Deklaration in der Arbeitsordnung, Rangfolge in `docs/work-packages.md` und die gespeicherte Ordnung in den Board-Daten sind EINE Aussage; weichen sie voneinander ab, blockt derselbe Wächter. Erfasst als Erweiterung von Arbeitsauftrag 440, der die Forderung bereits enthielt ("die Ordnung, die die Warteschlange rendert, MUSS die Ordnung des Pickers sein") und dem nur die Durchsetzung fehlte. Kein neuer Wächter — die Stelle, die "ist die Reihenfolge richtig?" beantwortet, existiert und beantwortet bisher nur die halbe Frage. |
| 3.85 | Das Bild, das seinen eigenen Beweis nicht führt | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, und es ist genau der, der hier durchgelassen hat: Der Verschluss in `scripts/verify/frameSubject.mjs` entscheidet die Wartefrage heute am MOTIV (`sceneReadyMode` in `scripts/verify/sceneReady-core.mjs` sagt "keine Wartezeit", sobald das Motiv ein Bedienelement ist), fotografiert aber die GANZE Seite, wenn die Erklärung weder Ausschnitt noch Element-Bindung trägt. Die Entscheidung: Die Wartefrage folgt künftig der AUFNAHME statt dem Motiv — wer ganzseitig auslöst, wartet wie jedes Szenenbild; nur ein beschnittener oder elementgebundener Griff behält den Sofort-Modus. Das ist als Arbeitsauftrag 518 (Ziffer 7) erfasst und wird dort gebaut; `scripts/verify/sceneReady.test.mjs` hält die Regel und ändert sich mit ihr. Kein neuer Wächter: Die Stelle, die das Bild schreibt, ist auch die Stelle, die seine Reife kennen muss. Die zweite Hälfte der Lektion — dass ein stilles Grün auf leerem Bild nicht unter "Last erzeugt falsche Rote" fällt — bleibt Urteilssache und ist in der Prosa festgehalten. |
| 3.86 | Der Wächter verlangte ein Pflaster, das es nicht geben konnte | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, und es ist der, der die Falschanklage erhoben hat: `scripts/ci-status-guard.mjs` kennt heute nur „rot" und „grün" und verlangt bei jedem Rot einen reparierenden Push. Die Entscheidung: Derselbe Wächter unterscheidet künftig, WO die Ursache liegt — ein Fehlschlag, dessen Meldung eine fremde Ursache nennt (steckengebliebene oder abgebrochene Auslieferung, Ablösung durch einen neueren Lauf), wird als solcher benannt und mit dem echten Griff versehen, statt eine Reparatur im Repository zu fordern, die es nicht gibt. Die zweite Hälfte — dass ein Lauf, der auf einen fremden Dienst wartet, sein Angefangenes zurücknimmt, statt eine Sperre zu hinterlassen — gehört in den Auslieferungslauf selbst. Beides ist als Arbeitsauftrag 526 erfasst und wird dort gebaut; `scripts/ci-status-guard-core.test.mjs` hält die Einordnung. Kein neuer Wächter: Die Stelle, die ein Rot beurteilt, ist auch die Stelle, die seine Zuständigkeit kennen muss. |
| 3.87 | Der Alarm, der auf ein Ereignis wartet, das ausbleibt | 1 | ZWEI Mechanismen, beide bereits als Arbeitsauftrag erfasst. Erstens: Die Überwachung hängt nicht mehr am roten Lauf, sondern am ZUSTAND — `scripts/deploy-staleness.mjs` vergleicht bei jedem Takt des Starters den ausgelieferten Stand der Seite (`build-info.json`, seit heute vom Build mitgestempelt) mit einem frisch geholten `origin/main` und schlägt auch dann an, wenn zum Stand des Hauptzweigs GAR KEIN Lauf existiert; `scripts/deploy-staleness-core.test.mjs` hält den Fall fest. Zweitens: Der Abgleich, der Board-Karten aus der Arbeitsordnung nachzieht, darf vorhandene Prosa nicht mehr überschreiben und meldet einen zu langen Block schon beim Import — als Arbeitsauftrag 530 erfasst. Kein neuer Wächter in beiden Fällen: Die Stelle, die ohnehin schreibt, ist die Stelle, die den Verlust verhindern muss. |
| 3.88 | Die Regel behauptet eine Reichweite, die der Mechanismus nicht hat | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, und zwar genau der, der hier durchgelassen hat: `isMechanismPath` in `scripts/mechanism-review-core.mjs` erkennt `-guard`, `-gate` und `scripts/git-hooks/*`, waehrend CLAUDE.md §7.2 zusaetzlich HAKEN nennt — acht verdrahtete Haken stehen damit ausserhalb des Vier-Augen-Tors, darunter der Text mit der hoechsten Frequenz im ganzen Projekt. Die Entscheidung: Das Muster wird auf `-hook` verbreitert (nicht die Regel auf den Code heruntergezogen), und `rule-review-state.countCorpusEntries` uebernimmt `guard-health-core.ENFORCER_RE` statt einer zweiten Definition. Als Arbeitsauftrag 535 erfasst. Dazu die Absicherung GEGEN DEN NAECHSTEN Fall derselben Klasse, denn eine einmalige Verbreiterung waere nur die Reparatur eines Symptoms: `scripts/guard-inventory-core.mjs` (aus Punkt 297) zaehlt bereits jeden verdrahteten Durchsetzer aus den echten Quellen und meldet, wessen Name kein Selektor erreicht — dieselbe Stelle vergleicht kuenftig die im Regeltext GENANNTEN Namensklassen mit den vom Muster tatsaechlich getroffenen und nennt die Differenz. Kein neuer Waechter: Die Stelle, die den Bestand aufzaehlt, ist auch die Stelle, die eine behauptete Reichweite gegen die wirkliche halten muss. |
| 3.89 | Die Attrappe war kleiner als die Wirklichkeit | 1 | Ein BESTEHENDER Durchsetzer deckt es bereits, und er ist auch der, der den Fehler gefunden hat: die Vitest-Pruefung `scripts/guard-preflight-core.test.mjs` faehrt jeden registrierten Waechter einmal gegen das ECHTE Repository statt gegen eine Attrappe und verlangt, dass kein Sammelschritt mit einem Fehlerstatus endet. Sie hat den Puffer-Tod in der Sekunde des Merges gemeldet. Die Entscheidung ist deshalb nicht ein neuer Waechter, sondern eine REGISTRIERUNGSPFLICHT: Ein Waechter zaehlt erst als gebaut, wenn sein Sammelschritt in dieser Registry steht — dann faehrt der Echtlauf ihn automatisch mit, und die Attrappen-Luecke schliesst sich fuer jeden kuenftigen Waechter von selbst. Zusaetzlich haelt `scripts/criticality-review-guard.test.mjs` den konkreten Fall fest, indem es das Arbeitsordnungs-Archiv in seiner WAHREN Groesse liest (gegengeprueft: ohne die Puffer-Korrektur wird der Fall rot). Was NICHT gedeckt ist und hier benannt gehoert: Der Echtlauf beweist nur, dass der Sammelschritt nicht stirbt — dass ein Waechter auf echten Daten auch das RICHTIGE entscheidet, prueft er nicht. Und die Pflicht ist heute erst zur Haelfte eingeloest: Die beiden Waechter aus Punkt 437 liegen absichtlich schlafend und sind noch nicht registriert — ihre Registrierung gehoert an ihre Scharfschaltung, und bis dahin faehrt der Echtlauf sie nicht mit. |
| 3.84 | Vier Recherchen, keine Messung — und die Antwort lag hinter einem Befehl | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, und es ist der, der ohnehin vor der Handlung spricht: `scripts/guard-preflight.mjs` beantwortet heute "würde ein Wächter blockieren". Die Entscheidung: Derselbe Aufruf fragt künftig bei einer DIAGNOSE-Handlung auch nach der Ursache — nennt die Sitzung eine Reihenfolge von Reparaturversuchen, ohne dass ein Befund die Ursache GEMESSEN hat, weist er darauf hin, dass der billigste Test zuerst gehört. Beratend, nicht blockierend: Ob eine Messung möglich ist, kann keine Maschine wissen, aber die Frage stellen kann sie. Die zweite Hälfte der Lektion — dass mehrere einige Meinungen keine Evidenz sind — bleibt bewusst mechanismusfrei; sie ist ein Urteil, kein Zustand.
| 3.83 | Die Stoppuhr, die auf einer anderen Maschine geeicht war | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, und er ist für genau diese Klasse gebaut: `scripts/verify/fixedWaits.mjs` zählt die festen Wanduhr-Wartezeiten je Suite gegen `fixed-wait-baseline.json` und lässt keine neue zu. Er behandelt aber jede gleich — und die vier roten Suiten zeigen, dass nicht der BESTAND das Risiko ist, sondern die Teilmenge, hinter der eine MESSUNG steht: Ein fester Schlaf vor einem Klick kostet Sekunden, einer vor einem Bildmaß erzeugt eine Falschanklage gegen das Produkt. Die Entscheidung: Derselbe Lauf weist die verbleibenden Wartezeiten künftig danach aus, ob im selben Block ein Screenshot oder eine Pixelmessung folgt, und nennt diese zuerst — die Rangfolge, nach der der Bestand abgetragen wird. Die Wartebedingung selbst ist gebaut und liegt bereit (`waitForSceneBuilt`, `waitForReadingStable` in `scripts/verify/_browser.mjs`), es fehlte nur der Zeiger darauf. Kein neuer Wächter: Die Stelle, die den Bestand zählt, ist auch die Stelle, die seine gefährliche Hälfte benennen muss. |
| 3.82 | Der Vergleichsstand, der die Startlogik mit dem Verglichenen teilt | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, und zwar der, der hier schon fast richtig gesprochen hat: `scripts/verify/baseline-classify.mjs` WARNT bereits, wenn die gemeinsamen Starthelfer seit dem Vergleichsstand gewandert sind („treat the verdict as advisory"). Die Entscheidung: Diese Warnung nennt künftig den RICHTIGEN Schnitt statt nur den Vorbehalt — sitzt die Änderung in der Messapparatur, ist der Schaltervergleich am GLEICHEN Stand das Werkzeug (hier `VERIFY_GALLIUM=none` gegen den Standard), nicht der Zeitvergleich. Kein neuer Wächter: Die Stelle, die den Verdacht ausspricht, ist auch die Stelle, die den Ausweg kennen muss. Die zweite Hälfte der Klasse — dass der volle Lauf nach dem Umzug schlicht niemand mehr gefahren hat — ist als Arbeitsauftrag 499 erfasst und gehört zur CI-Sichtbarkeit (Punkt 387), nicht hierher. |
| 3.80 | Die Reparatur, die den Reparierenden mitnimmt | 2 | Ein NEUER Durchsetzer, weil kein bestehender die Befehlszeile beurteilt, BEVOR sie die Infrastruktur zerlegt, über die die Sitzung arbeitet: `scripts/firewall-guard.mjs` (Kern `scripts/firewall-guard-core.mjs`) verweigert als PreToolUse-Hook den Neubau-Aufruf, `iptables -F/-X/-P`, `ipset destroy`, `iptables-restore` und ihre Verwandten und nennt in der Absage die beiden erlaubten Wege. Er steht BEWUSST für nichts zurück — anders als jeder Stop-Wächter dieses Projekts, weil ein zugesperrter Container die Sitzung tötet, die den Befehl absetzt, ob sie den Batch hält oder nicht. Dazu die zwei Werkzeuge, ohne die eine Absage nur eine Sackgasse wäre: `scripts/firewall-allow.mjs` erweitert die Sperrliste rein additiv (keine Regel, keine Sperre, kein Fenster ohne Netz), und `scripts/firewall-rebuild.mjs` führt den Neubau abgekoppelt und fail-OFFEN aus. Arbeitsauftrag 496, gebaut, gegengeprüft und scharf. |
| 3.81 | Die Regel, die gar keinen Mechanismus hatte — und die Prüfung, die das nicht sah | 2 | Ein NEUER Durchsetzer für die Regel selbst, und eine Verbreiterung für die Prüfung darüber — beides als Arbeitsauftrag 497 erfasst. Zur Regel: Kein bestehender Wächter beurteilt die SPRACHE der Antwort; `timestamp-guard` liest den fertigen Antworttext bereits und ist damit die Schicht, auf der der neue reitet, aber seine Entscheidung ist eine andere, also wird er nicht überladen. Zur Prüfung: `docs/rule-corpus-audit.md` bekommt die Achse „was misst das eigentlich?" für JEDE Zeile, weil genau ihr Fehlen diese Regel als „OK" durchgewinkt hat — eine leere Befundspalte bedeutete bisher „Text stimmt", nie „wird gemessen". `guard-health-guard` bleibt unangetastet: Er beantwortet die Nachbarfrage (kann ein verdrahteter Durchsetzer feuern?) korrekt und vollständig; die Lücke liegt zwischen den beiden Prüfungen, nicht in einer von ihnen. |
| 3.79 | Die Scharfstellung war geerbt, nicht versioniert | 1 | Ein BESTEHENDER Durchsetzer wird erweitert, und es ist genau der dafür gebaute: `scripts/guard-health-guard.mjs` steht unter der Überschrift „no enforcer may sit in the tree unable to fire" und liest die Hook-Dateien des aktiven Verzeichnisses bereits ein — er liest aber nur ihren INHALT und fragt nie, ob sie überhaupt ausführbar sind. Genau daran ist das Push-Tor gescheitert. Die Entscheidung: derselbe Lauf beurteilt künftig auch die Scharfstellung eines Hooks, nicht nur seine Anwesenheit; ein Hook im aktiven Verzeichnis ohne Ausführungsrecht ist ein BEFUND wie ein unverdrahteter Guard. Die Reparaturhälfte — das Recht beim Wiring setzen, im Arbeitsbaum und im Index — ist Arbeitsauftrag 495. Kein neuer Wächter: Die Stelle, die „kann dieser Durchsetzer feuern?" beantwortet, existiert und hat die Frage bisher nur zur Hälfte gestellt. |
| 3.78 | Die Arbeit, die an den Nutzer zurückgereicht wurde | 3 | LÜCKE, offen gemeldet statt stillschweigend als "keine nötig" verbucht: Kein bestehender Durchsetzer liest die ANTWORT an den Nutzer — die Wächter dieses Projekts beurteilen Arbeitsbaum, Board und Commits, und kein einziger prüft, was die Sitzung dem Menschen aufträgt. Eine Verbreiterung kommt deshalb nicht in Frage; es braucht einen neuen, und er ist als Arbeitsauftrag 494 erfasst und delegiert: Ein Zug-Ende wird geblockt, wenn die Antwort dem Nutzer einen Schritt INNERHALB des Containers aufträgt (Installation, Paketmanager, Skriptaufruf, Dateiänderung im Arbeitsbereich), während die einmalige Bitte um eine fehlende FÄHIGKEIT erlaubt bleibt — sonst drängt der Mechanismus ins stille Scheitern. Bis er scharf ist, trägt die Regel allein das Memory `container-work-is-mine`, und das ist genau die Schwäche, die diese Zeile benennt. |
| 3.68 | Der Test, der nur im Nebenbaum grün ist | 2 | Ein NEUER Durchsetzer, weil kein bestehender Testhygiene beurteilt: Das schnelle Tor nach dem Merge würde den Fall finden — aber erst, wenn `main` schon rot ist, und genau diesen Preis soll die Prüfung vor dem Merge sparen. Die Entscheidung: eine reine Prüfung in der Vitest-Schicht nach dem Muster der Vollständigkeits-Tore dieses Projekts (`src/config/quality.test.ts`), die einen Testdatei-Diff daran festhält, dass er seine Pfade EINGESPRITZT bekommt statt echte Repo-Pfade zu lesen — ein Test, der `.claude/` oder einen git-ignorierten Pfad direkt anfasst, misst im Nebenbaum systematisch etwas anderes als dort, wo er später laufen muss. Erfasst im Bündel **Testinfrastruktur**. Die Urteilshälfte („wäre er auch im Hauptbaum grün?") bleibt bewusst beim Menschen: welcher Laufzeitzustand außerhalb des Repositoriums liegt, ist keine Musterfrage. |
| 3.67 | Das Review, das seinen Autor überlebt | 1 | Ein BESTEHENDER Durchsetzer wird erweitert: `scripts/mechanism-review-guard.mjs` verlangt die Gegenprüfung des anderen Modells schon — er fragt aber nur, OB ein Urteil aufgezeichnet ist, nicht WAS es sagt. Die Entscheidung: Ein Urteil „nicht mergen" oder „mit Korrekturen" erfüllt das Tor NICHT mehr; erfüllt ist es erst durch ein späteres Urteil über einen späteren Commit — die Polarität und die Reihenfolge werden Teil der Bedingung, nicht nur die Existenz der Zeile. Dazu die zweite Hälfte im Auftrag des Agenten (`scripts/point-brief.mjs`): Wer ein Review beauftragt, bleibt im Zug, bis es da ist — dieselbe Stelle, an der die Commit-je-Schritt-Zeile schon lebt und die am 27.07.2026 der Unterschied zwischen einem Agenten war, der alles verlor, und einem, der nichts verlor. Erfasst im Bündel **Modell & Wächter**. Anlass: drei Fälle an einem Vormittag, einer davon hätte den Hauptzweig rot gemacht. |
| 3.62 | Die Rettung, die den Schaden verstärkt hätte | 1 | Ein BESTEHENDER Durchsetzer trägt es schon: `scripts/mechanism-review-guard.mjs` verlangt die Gegenprüfung durch das andere Modell für jeden neuen oder geänderten Mechanismus. Was fehlte, war der ZEITPUNKT — geprüft wurde bisher das Ergebnis, hier hat die Prüfung am ENTWURF eine Lücke gefunden, die kein Test gezeigt hätte. Erfasst in Punkt 434 als Arbeitsweise (Design-Review vor der ersten Zeile, im Register verbucht) und inhaltlich in Punkt 433 als Pflichtteil: Umgebungs-Preflight vor dem Spawn, Fehlerzähler auch für den lebenden-aber-stillen Nachfolger, eskalierender Backoff. Kein neuer Wächter — die vorhandene Pflicht wird nach vorne verlegt. |
| 3.61 | Zwei Stunden erkannt, zwei Stunden nur notiert | 1 | GEBAUT (Arbeitsauftrag 433, 30.07.2026). Ein BESTEHENDER Durchsetzer wurde erweitert, nicht ergänzt: `scripts/batch-autostart.mjs` stellte „WEDGED owner" schon fest — ihm fehlte die Folge. Jetzt gibt dieselbe Stelle den Lock frei und startet den Nachfolger (`wedgeTakeover` + `acquire({takeWedged})`, derselbe atomare Test-and-Set, im Reap-Mutex nachgeprüft); die Schwelle sank von vier Stunden auf **45 Minuten**, gemessen an den 32 440 Werkzeugaufrufen der Projekt-Transkripte (p99,9 = 10 min, längster unangekündigter unbeaufsichtigter Aufruf 27,8 min) statt geraten; die Bedingung „nur der eigene Spawn" fiel — genau sie ließ das Urteil jener Nacht auf eine Log-Zeile durchfallen, denn der Owner war von Hand gestartet; und eine mehrfach gleichlautende Feststellung eskaliert einmal und verstummt dann (`verdictRepeat`). Dazu die Hälfte ohne die 433 es schlimmer machte (Vier-Augen-Review): Umgebungs-Preflight vor dem Spawn, `failCount` zählt den lebenden-aber-nutzlosen Nachfolger, eskalierender Backoff — sonst wäre eine stille Nacht eine laute Kette von Atmenden geworden. Vitest: `scripts/batch-singleton-core.test.mjs`, `scripts/batch-autostart-core.test.mjs`. **ÜBERHOLT (Punkt 434, 30.07.2026):** Der Besitz am Batch ist seither eine LEASE — `leaseUntil` am Lock, vor jedem Werkzeugaufruf verlängert, Ablauf reine Rechnung. `wedgeTakeover`, `takeWedged`, `WEDGED_MS` und die zweistufige Stille-Meldung existieren nicht mehr; die 45-Minuten-Messung lebt als Kalibrierung des Lease-Fensters weiter, und die Lehre selbst — ein Urteil braucht eine Folge — gilt unverändert. Ein PreToolUse-Zaun verweigert einer abgelösten Sitzung zusätzlich Merge/Push, den Haken, die Tafel und den gemeinsamen Zustand. Siehe `docs/batch-resilience.md` §3/§6. |
| 3.60 | Der Befund, der nur so lange lebte wie das Fenster | 2 | Ein NEUER Durchsetzer, weil kein bestehender diese Klasse erreicht: Die Wächter dieses Projekts beurteilen alle einen Zustand, den die Sitzung geschrieben HAT — hier fehlt der Zustand gerade deshalb, weil die findende Sitzung nicht schreiben durfte. Erfasst als Arbeitsauftrag 432: `scripts/finding.mjs` schreibt gegen einen Träger im Memory-Verzeichnis, das in JEDEM Zustand beschreibbar ist, und `scripts/findings-guard.mjs` (reiner Kern `scripts/findings-core.mjs`, Vitest, fail-open) blockt das Zug-Ende bei zwei Bedingungen: ein Zug, der untersucht und nichts Dauerhaftes hinterlassen hat, und eine Sitzung, die den Batch hält, während der Träger noch gefüllt ist — die zweite Hälfte ist die entscheidende, denn ohne Leerungspflicht verschiebt der Mechanismus den Verlust nur von der Sitzung auf die Datei. |
| 3.58 | Ein Agent in Arbeit, zwei Plätze leer | 1 | GEBAUT (Arbeitsauftrag 427, 30.07.2026): kein neuer Wächter, sondern eine UNTERGRENZE in dem, der die Wartezeit ohnehin beurteilt. `scripts/batch-in-flight.mjs` zählt die Agenten, die die Wartemeldung selbst SICHTBAR macht (`declaredAgentCount` über Worktrees und Zweige), liest den offenen Arbeitsauftrag und fragt `slotReasonDecision`; bei freien Plätzen und einem unabhängigen offenen Punkt verlangt es `--slots-free "<Grund>"`, und `scripts/batch-progress-guard.mjs` blockt bis dahin (`block-slots-free`, Wortlaut in `slotsRemedy` festgenagelt). Die Gegenprobe zum Nörgeln ist eingebaut: voller Pool, eine Warteschlange deren Restpunkte alle Dateien des laufenden Zweigs berühren, `.claude/batch-paused` und ein laufender Closing-Freeze brauchen gar keinen Grund — erkannt an der Closing-Checkliste, die `closing-guard` für den AKTUELLEN Head ohnehin führt (`closingFreezeActive`), nicht an einer Markierungsdatei, die niemand schreibt — und ein Punkt, dessen Spec KEINE Datei nennt, ist nie ein Kandidat, ebenso wie eine unlesbare Dateiliste nie eine Forderung erzeugt. Und `--status` lügt nicht mehr: das Kommando verspricht „was der Stop-Hook entscheiden würde", und der neue Block ist erreichbar, während der Nachweis einwandfrei lebt — `statusVerdict` entscheidet beides aus einer Quelle. Vitest: `scripts/batch-in-flight-core.test.mjs`. |
| 3.90 | Der Prüfer, der die fertige Liste abhakt | 3 | TEILWEISE, und die offene Hälfte wird benannt statt stillschweigend als „bewusst keine" verbucht. Gedeckt ist der KONVERGENTE Schritt: `scripts/mechanism-review-guard.mjs` lässt keinen neuen oder geänderten Mechanismus ohne das aufgezeichnete Urteil des ANDEREN Modells durchgehen — genau die Form, für die das Gegenlesen das richtige Werkzeug ist. LÜCKE ist der DIVERGENTE Schritt: Nichts stellt fest, ob ein aufzählender Schritt (was kann schiefgehen, welche Fälle prüfen, welche Entwürfe sind möglich) blind parallel gelaufen ist oder als Gegenlesung einer fertigen Liste — und maschinell FESTSTELLEN kann es auch keiner, denn ob ein Schritt divergent war, steht in keiner Datei. Was billig geht und die bevorzugte Antwort ist, weil es einen BESTEHENDEN Durchsetzer verbreitert statt einen neuen danebenzustellen: Das Aufzeichnungs-Werkzeug `scripts/mechanism-review.mjs --record` nimmt die BETRIEBSART mit auf (Gegenlesung oder blind parallel), sodass ein Urteil, das einen Fundschritt abdecken soll, seine Form nennen muss; und `scripts/guard-preflight.mjs` stellt vor einem solchen Schritt beratend die Frage. Beides ist seit 07.08.2026 GEBAUT: `--mode` ist Pflicht zum SCHREIBEN eines Urteils (nie zum Lesen, die Alt-Zeilen bleiben gültig), `--framing` trägt die schwächere Ein-Modell-Rückfallform, und die Vorabfrage stellt die Frage beratend, ohne je zu blockieren. Was die Betriebsart NICHT leistet und auch nicht leisten kann: ob ein Schritt wirklich blind parallel lief, bleibt eine Selbstauskunft — das Werkzeug erzwingt, dass sie ABGEGEBEN wird, nicht dass sie stimmt. |
| 3.91 | Die Reparatur, die in die stille Richtung überschießt | 1 | Ein BESTEHENDER Durchsetzer deckt den Kern, und er ist auch der, der den Fehler gefunden hat: `scripts/mechanism-review-guard.mjs` lässt keine Änderung an einem Wächter ohne das aufgezeichnete Urteil des ANDEREN Modells enden — genau der Griff, der hier zwölf stillgelegte Richtig-Positive ans Licht brachte, während die Suite grün war. Was der Durchsetzer NICHT erzwingen kann und worauf die Lehre deshalb hinweist: dass der Prüfer an der MECHANIK misst und nicht an den Testnamen, und dass er die Gegenrichtung mit Fällen prüft, die nicht aus der Feder des Autors stammen. Das steht im Prüfauftrag, nicht im Wächter, und ist dort auch richtig aufgehoben — eine Maschine kann nicht feststellen, ob eine Probe unabhängig gedacht war. KEIN neuer Wächter: Was zu bauen wäre, prüfte die Sorgfalt eines Urteils, und ein Wächter, der Sorgfalt behauptet, ohne sie messen zu können, ist genau die Attrappe aus §3.89. |

---

## Offene Durchsetzungslücken (Stand 27.07.2026)

Aus der Rückerfassung, nicht stillschweigend als „bewusst keine" abgelegt:

1. **3.20 — Beweisliste nach einem Zwischenfall.** Die Form ist gebaut und
   erzwungen, aber nur für die Auslieferung (`scripts/closing-guard-core.mjs`).
   Die billige Fassung ist eine zweite Schrittliste im **selben** Guard — kein
   neuer Guard.
2. **3.35 — Präsens-Zusicherungen im Auftrag.** Ungebaut
   (`docs/rule-corpus-audit.md` D-i). Der Kurzbrief prüft Verweise, nicht
   Behauptungen — und eine falsche Tatsachenbehauptung führt zu einer Lieferung,
   die genau nichts tut.
3. **3.8 — lastabhängige Zeitschranken.** Ungebaut
   (`docs/rule-corpus-audit.md` D-h). Nur die Urteilshälfte dieser Lektion ist
   bewusst mechanismusfrei.
4. **3.39 — Commit je Schritt.** Die Sicherung ist erzwungen, die
   **Schrittweite** nicht: Sie lebt in handgeschriebenen Prompts. Am 27.07.2026
   war genau diese Zeile der Unterschied zwischen einem Agenten, der alles
   verlor, und einem, der nichts verlor — der billigste denkbare Ort dafür ist
   der generierte Kurzbrief.
| 3.92 | Der Wert, der beim Ausliefern schon abgelaufen war | 1 | Ein BESTEHENDER Durchsetzer deckt die Haelfte, an der es heute scheiterte, und zwar vollstaendig: `scripts/timestamp-guard.mjs` liest den fertigen Antworttext und blockt jede Minute, die nicht der gemessenen Berliner Zeit entspricht — er hat in dieser Nacht dreimal richtig geblockt, es ist ihm nichts durchgerutscht. Die Lehre richtet sich deshalb nicht gegen den Waechter, sondern gegen die QUELLE: Der Einspeise-Haken `scripts/dashboard-reminder-hook.mjs` schreibt die Zeit an den Zug-ANFANG, und ich habe sie am Zug-ENDE als Messung gelesen. Verbreitert statt neu gebaut wird deshalb der Haken, nicht der Waechter — sein Zeitstempel wird als das benannt, was er ist (der Stand bei Zugbeginn, nicht die aktuelle Zeit), damit er nicht als Messung durchgeht. LUECKE, benannt statt stillschweigend verbucht: Dieselbe Falle steckt im Zwischenspeicher von `scripts/ci-status-guard.mjs`, dessen eingefrorener Grundtext in derselben Nacht dreimal einen laengst gruenen Lauf als „laeuft noch" nannte und zwei Zuege Fehlsuche kostete. Das Urteil des Waechters war jedes Mal richtig, nur seine Begruendung war alt; als eigener Punkt erfasst. |
| 3.93 | Die eingestandene Grenze, die niemand nachprüft | 1 | Ein BESTEHENDER Durchsetzer deckt es, und er ist auch der, der diesen Fall ueberhaupt vor ein zweites Modell brachte: `scripts/mechanism-review-guard.mjs` laesst keine neue oder geaenderte Absicherung enden ohne das aufgezeichnete Urteil des ANDEREN Modells. VERBREITERT statt neu gebaut wird der PRUEFAUFTRAG, den dieses Urteil einloest: Er nennt die vom Autor EINGERAEUMTEN Grenzen jetzt ausdruecklich als zu widerlegende Behauptungen und verlangt die GEGENPROBE am alten Stand als Teil des Nachweises, dass eine Luecke zu ist. Beides ist Text im Auftrag, nicht Code — und dort richtig aufgehoben: Ob ein Pruefer eine eingeraeumte Grenze wirklich angefasst hat, kann keine Maschine feststellen; was sie feststellen kann, ist, dass ein Urteil ueberhaupt vorliegt, und das tut sie. KEIN neuer Waechter: Zu bauen waere einer, der die Gruendlichkeit eines Urteils behauptet, ohne sie messen zu koennen — die Attrappe aus 3.89. |
| 3.94 | Die Isolierung trennt auch das ab, was ankommen sollte | 1 | Ein BESTEHENDER Durchsetzer ist betroffen, und zwar als Geschaedigter: `scripts/render-verify-guard.mjs` fragt richtig, ob dieses Backend seit der letzten Render-Aenderung bewiesen wurde — nur liest er sein Buch an einem Ort, den `scripts/repo-paths.mjs` aus dem SKRIPTPFAD herleitet, also einmal je Arbeitskopie. VERBREITERT wird deshalb die Ablage, nicht die Regel: Der Ledger-Pfad, den `scripts/render-verify-state.mjs` ueber `scripts/repo-paths.mjs` bildet, wird gegen das gemeinsame Git-Verzeichnis aufgeloest, damit ein Lauf aus einem Worktree im Hauptbaum ankommt und das Aufraeumen ihn nicht mitloescht; die Deckungsfrage des Waechters bleibt unveraendert, es kommt keine Ausnahme hinzu. KEIN neuer Waechter: Zu bauen waere einer, der einen zweiten Ort bewacht, statt den einen richtig zu waehlen. ERFASST als Arbeitsordnungspunkt 558 (08.08.2026) — bis der landet, ist die Luecke benannt und nicht stillschweigend verbucht: Jeder delegierte Render-Punkt kostet den Hauptbaum entweder einen zweiten Lauf oder eine Zurueckstellung fuer ein Bild, das es gibt. |
| 3.95 | Wer ein Signal abschaltet, erbt die Pflicht, das Ersatzsignal zu prüfen | 1 | Ein BESTEHENDER Durchsetzer ist betroffen, und zwar als Getäuschter: `scripts/ci-status-guard.mjs` urteilt über das ERGEBNIS eines Laufs — und genau dieses Ergebnis macht die Dämpfung auf Arbeitszweigen absichtlich grün. Gemessen wurde dabei mehr, als der Umbau beabsichtigte: Die Schnittstelle meldet auch den fehlgeschlagenen EINZELSCHRITT als erfolgreich, sodass kein Ergebnis-Leser die Wahrheit mehr rekonstruieren kann; sie steht nur noch im Commit-Status. VERBREITERT wird deshalb nicht die Dämpfung, sondern die Ablesestelle: Der Merge nach `main` liest den Zweig-Status des Zweigkopfs, bevor er zulässt (Arbeitsordnungspunkt 561a); die Zugende-Prüfung auf Zweigen bleibt bewusst offen, weil Rot auf einem Zweig laut Nutzerentscheidung normal ist. LÜCKE, benannt statt stillschweigend verbucht: Der verbleibende aktive Meldeweg — die Push-Nachricht der Werkstatt — ist gar nicht scharf, sein Zugangsschlüssel wurde nie gesetzt; als Punkt 560 erfasst. KEIN neuer Wächter: Zu bauen wäre einer, der eine zweite Meinung über dasselbe Laufergebnis bildet, das die Quelle bereits verfälscht hat. |
| 3.96 | Die Fieberprobe, die den Patienten anhält | 1 | Ein BESTEHENDER Mechanismus ist der Verursacher: Die Tafel-Erreichbarkeitsprobe in `scripts/batch-autostart.mjs` eskaliert über fünf Alarme bis zur bewussten Pause des Stapels — die schärfste Stufe, die dieses System kennt. VERBREITERT wird die Probe, nicht die Eskalation: ein sofortiger Wiederholversuch, bevor ein Fehlschlag überhaupt zählt, Zählung nur AUFEINANDERFOLGENDER Fehlschläge (ein Erfolg setzt zurück) und eine Meldung, die Leitung und Inhalt auseinanderhält, weil ein fehlgeschlagener Abruf keine Aussage über die Aktualität der Tafel ist. Die Pause bleibt als letzte Stufe erhalten — sie ist für einen echten Ausfall das richtige Werkzeug. Als Arbeitsordnungspunkt 562 erfasst (08.08.2026), Kritikalität hoch für den unbeaufsichtigten Betrieb: Es ist der eine Fehlermodus, der alle anderen Punkte blockiert. KEIN neuer Wächter: Zu bauen wäre einer, der eine falsch auslösende Probe überwacht, statt sie richtig zu stellen. |
| 3.97 | Der Mechanismus war gebaut, dokumentiert — und wurde nie gefüttert | 1 | Ein BESTEHENDER Durchsetzer deckt es, und er nennt den Handgriff sogar in seiner eigenen Blockmeldung: `scripts/render-verify-guard.mjs` laesst keinen Lauf als Deckung zaehlen, dessen rote Pruefungen nicht je einem OFFENEN Punkt zugeschrieben sind, und sagt dazu woertlich, wohin die Zuschreibung gehoert (`scripts/render-verify-charges.mjs`) und dass ein herrenloses Rot ein BEFUND ist und kein Registereintrag. Genau so hat er am 09.08.2026 funktioniert: Zwei herrenlose Fehlschlaege bekamen erst ihre Punkte (568, 570), dann ihre Eintraege. NICHT gedeckt ist der Fall, an dem es scheiterte, und deshalb steht er als Lehre da: Ein Punkt, der auf ein gruenes Tor WARTET, ohne dass jemand den Lauf treibt, erreicht den Waechter nie — die vier gemessenen und je einem offenen Punkt zugeordneten Rotmeldungen standen als Fliesstext in der Messung und nie im Register, und Punkt 309 wartete dadurch auf ein Tor, das nicht aufgehen konnte. VERBREITERT wird deshalb nichts am Waechter: Die Entscheidung — die vier Wackler reparieren oder sie ordentlich verbuchen — ist am 09.08.2026 in Arbeitsordnungspunkt 200 festgeschrieben, wo die Messung ohnehin liegt. KEIN neuer Waechter: Zu bauen waere einer, der die Wartegruende offener Punkte auf Wahrheit prueft — er muesste raten, worauf ein Punkt wartet, und genau das steht als Prosa da, nicht als Datum. |
| 3.98 | „Nicht gelaufen" sieht aus wie „korrekt abgelehnt" | 3 | LÜCKE, mit Termin: KEIN bestehender Durchsetzer deckt es, und das ist nachprüfbar statt behauptet — die Wächterfamilie beurteilt Zustände des Arbeitsstands (Tafel, Zweige, Regeln, Bilder), keiner von ihnen liest, WIE ein Test seine Zusicherung bildet. Der Fehler liegt eine Ebene tiefer, in der Testschicht selbst, und er ist dort maschinell erkennbar: Ein Test, der einen Fremdprozess startet und aus einem Rückgabewert ≠ 0 auf „hat abgelehnt" schließt, ohne zu prüfen, dass der Prozess überhaupt lief, ist ein Muster und kein Urteil. Gefunden wurde er in `scripts/verify/scope.test.mjs`. Der Durchsetzer wird ein reines Tor in der schnellen Testschicht — dieselbe Bauform wie der Bildbetreff-Zwang, ausdrücklich KEIN weiterer Stop-Hook — und ist als Arbeitsordnungspunkt 573 (09.08.2026) mit genau diesem Endzustand erfasst: „nicht gestartet" wird ein eigener, benannter Fehlschlag, derselbe Griff wird über alle übrigen Fundstellen gezogen, und das Tor hält das Muster fern. Bis dieser Punkt gebaut ist, steht die Lehre OHNE Durchsetzung da, und diese Zeile sagt es, statt es als bewusstes „keiner" auszugeben. Warum nicht sofort erweitern: Es gibt nichts zu erweitern — der nächste Fundort meldet sich nicht von selbst, denn die stille Hälfte dieses Fehlers ist grün. |
| 3.99 | Der Haken misst den Punkt an seinem Gefühl, nicht an seiner eigenen Liste | 1 | Ein BESTEHENDER Durchsetzer deckt genau das und ist nur zu schmal gestellt: `scripts/point-proof-guard.mjs` verweigert den Haken, solange eine `PROOF:`-Zeile des Punktes nicht am aktuellen HEAD gelaufen ist — die Bauform ist also da, samt Register und `--status`. Ihre Schwäche ist die Freiwilligkeit: Wer eine Spezifikation schreibt, denkt an das, was er bauen will, nicht an das, woran man ihn später misst, und ein Punkt ohne `PROOF:`-Zeile wird an seinem Gesamteindruck abgehakt — bei Punkt 572 an einem großen, guten Dokument, das die beiden fehlenden Drittel überstrahlte. VERBREITERT wird er deshalb um die Liste, die der Punkt ohnehin schon enthält: Zählt eine Spezifikation ihre Liefergegenstände auf (`DELIVERABLES:`, `(1)…(2)…`, ein nummerierter `FINAL STATE`), so druckt der Wächter beim Haken genau diese Aufzählung und verlangt je Eintrag eine Zeile Beleg über denselben `--ran`-Weg, den es schon gibt. Kein neuer Hook, kein Geschwister-Guard: dasselbe Register, dieselbe Blockstelle, nur nicht mehr abhängig davon, dass der Autor an sie gedacht hat. Bis die Verbreiterung gebaut ist, deckt die freiwillige Hälfte weiter, was sie heute deckt. |
| 3.100 | Gebaut, getestet, dokumentiert — und in den Weg gelegt hat es niemand | 3 | LÜCKE, mit Termin, und die Begründung ist nachprüfbar statt behauptet: KEIN bestehender Durchsetzer kann es fangen, weil hier nichts VERLETZT wird. Die Wächterfamilie beurteilt Zustände, die von einer Regel abweichen — eine Fähigkeit, von der niemand erfährt, weicht von keiner ab, und alle Prüfungen bleiben grün. Verbreitern lässt sich auch nichts: Der nächste unbenutzte Mechanismus meldet sich nicht, denn Schweigen ist genau sein Symptom. Erfasst als Arbeitsordnungspunkt 602 (09.08.2026) mit drei Teilen in aufsteigenden Kosten: (1) eine REGEL — wer eine Fähigkeit baut, benennt im selben Commit die Stelle, an der jemand danach greift; (2) jede Fähigkeit erklärt bei der Lieferung ihr NUTZUNGSSIGNAL, sonst ist die Frage „wird das benutzt?" überhaupt nicht beantwortbar; (3) ein BERICHT im Abschlusslauf listet, was seit der Lieferung nie angesprungen ist. Ausdrücklich KEIN blockierender Wächter: Ein untätiger Rückfall ist kein Defekt, und ein Tor, das darauf feuert, erzöge dazu, gar kein Signal mehr zu erklären (§3.32). Bis Punkt 602 gebaut ist, steht die Lehre ohne Durchsetzung da, und diese Zeile sagt es. |

| 3.101 | Der Regler war da — unter der falschen Überschrift | 2 | Ein NEUER Durchsetzer, aber der billigsten Bauform und bereits mit dem Punkt geliefert: ein reines Tor in der schnellen Testschicht (`src/ui/DebugMenu.test.tsx`), das die Regel „jedes Bedienelement, dessen Kennung auf `Volume` endet, sitzt in der Ton-Gruppe und nirgends sonst" über die Vollständigkeitstabelle UND das gerenderte Menü prüft. Ausdrücklich KEIN Stop-Hook: Die Eigenschaft ist am Artefakt selbst entscheidbar, also gehört sie in die Testschicht (dieselbe Bauform wie der Bildbetreff-Zwang). Verbreitert wurde nichts, weil es nichts zu verbreitern gab — kein bestehender Wächter urteilt über die EINSORTIERUNG eines Bedienelements, sie alle prüfen Zustände des Arbeitsstands. LÜCKE, benannt statt stillschweigend verbucht: Gedeckt sind damit die Lautstärken, nicht die allgemeine Lehre. Ob eine neue Einstellung bei ihresgleichen steht, ist für die übrigen Familien (Sichtweite, Empfindlichkeit, Zeitwerte) heute unbewacht, und der Weg des Spielers „von dort, wo ich suche, zu dem, was ich brauche" wird von keiner Prüfung gegangen — sie prüfen Zustände, keine Wege. Bis dafür eine Bauform gefunden ist, die mehr misst als eine Namensendung, steht dieser Teil ohne Durchsetzung da, und diese Zeile sagt es. |

| 3.102 | Der Sammelbefehl erbt die Pflichten der Befehle, die er ersetzt | 1 | BESTEHENDE Durchsetzer, verbreitert statt neu gebaut, und zwar an der Stelle, an der sie blind waren: `scripts/batch-lease-core.mjs` (Zugriffssperre), `command-classify-core.mjs` (Tafel-Vorabsperre) sowie `closing-guard` und `point-proof-guard` kennen den Landebefehl jetzt NAMENTLICH — bei den beiden letzten ueber die gemeinsame Haken-Erkennung `landingTickNumber`, sodass ein weiterer Wrapper an einer Stelle nachgetragen wird und nicht an vieren. Kein neuer Waechter: Zu bauen waere einer, der raet, welche Befehle ein Skript ausfuehrt — nicht entscheidbar, und die Attrappe aus 3.89. Die Reihenfolge (dauerhaft machen vor loeschen) haelt ein TEST in der schnellen Schicht, nicht eine Regel. LUECKE, benannt: Die Namensliste ist handgepflegt, also faellt der naechste Wrapper wieder durch, bis ihn jemand eintraegt; und `point-proof-guard` ist ueberhaupt nicht verdrahtet (Arbeitsordnungspunkt 609), sodass seine frische Kenntnis des Befehls erst beisst, wenn er scharf gestellt wird. |

| 3.103 | Eine korrekte Übergabe, die niemand aufnimmt, ist ein Stillstand | 1 | BESTEHENDE Durchsetzer, verbreitert statt neu gebaut, an beiden Stellen, an denen sie heute blind sind. (a) Die Pacht (`scripts/batch-lease-core.mjs`) fragt bereits, ob die angemeldete Arbeit VORANKOMMT — nur erst nach einer Stunde. Arbeitsordnungspunkt 612 zieht dieselbe Arithmetik auf ein kurzes Untätigkeitsfenster vor und macht die FREIGABE zum Ereignis, auf das der Starter reagiert; der 15-Minuten-Takt bleibt Rückfallebene. Kein neuer Wächter: Die Entscheidung ist am Sperrzustand ablesbar, gehört also dorthin, wo sie schon getroffen wird. (b) Die Reihenfolge wird aus EINER Quelle abgeleitet — Punkt 590 (der 608 aufnimmt) streicht die handgeführte Liste in den Tafeldaten und lässt die gerenderte Reihenfolge aus der Arbeitsordnung entstehen, mit einem Wächter auf der Abweichung. LÜCKE, benannt statt stillschweigend verbucht: Bis 590 liegt, prüft NICHTS, ob die drei Orte (Arbeitsordnung, dokumentierte Rangfolge, Tafel-Reihenfolge) dasselbe sagen — genau die Spaltung, an der der Nutzer die Neusortierung zweimal nicht sah. Und der Beweis, dass die Kette einen Ausfall zu einem beliebigen Zeitpunkt übersteht, hängt weiter am ungebauten Chaos-Test (449). |

| 3.104 | Eine Sitzung wächst an Anliegen, nicht an Punkten | 1 | BESTEHENDER Durchsetzer, verbreitert statt neu gebaut: Arbeitsordnungspunkt 553 baut bereits ein gemessenes Kontextbudget mit schriftlicher Übergabe auf `scripts/batch-progress-guard.mjs` und `scripts/measure-context-cost-core.mjs` — es kannte nur den DELEGIERTEN Punkt. Der Punkt trägt jetzt den zweiten Fall: Derselbe Zähler zählt die begleitete Sitzung, und jenseits der Grenze verlangt die Stop-Kette entweder Übergabe plus Grenzstein oder einen genannten Grund weiterzumachen; ein NEUES Thema wird angehängt statt begonnen. Kein neuer Wächter, weil der Zähler und die Stop-Kette existieren und nur der zweite Fall fehlte. LÜCKE, benannt: Bis 553 gebaut ist, gibt es gegen dieses Muster GAR KEINE Durchsetzung — der Grenzstein hängt weiter an einem abgeschlossenen Punkt, den eine begleitete Sitzung mitten im Gespräch nie erreicht. |

Teil-Deckungen, die keine Lücke im Sinne dieser Liste sind, aber in der
periodischen Regel-Durchsicht wieder aufzurufen sind: 3.5 (nur die sichtbare
Klasse), 3.21 (nur dokumentseitig, nicht im Memory-Korpus), 3.34 (nur die
Guard-Kette), 3.36 (der wirksame Teil liegt außerhalb des Repos), 3.31 (beide
genannten Werkzeuge sind beratend, nicht blockierend).

## Wie diese Liste entstanden ist

Die Rückerfassung wurde nach dem Vier-Augen-Prinzip gegengelesen (ein zweites
Modell, das jede genannte Datei geöffnet und gegen die Lektion geprüft hat).
Diese Prüfung fand genau das, was die maschinelle nicht finden kann: **drei
Zeilen, deren benannter Durchsetzer die Lektion nicht deckt** (3.39 verwies auf
eine Zeile, die es im Kurzbrief nicht gibt; 3.35 verbuchte die Deckung einer
Nachbarlektion; 3.36 nannte einen Wächter, der nichts blockiert) und zwei falsch
kodierte Ergebnisse (3.34, 3.37). Das ist der Beleg für den Vorbehalt weiter
oben — die Existenz einer Datei ist billig prüfbar, ihre Deckung nicht.
